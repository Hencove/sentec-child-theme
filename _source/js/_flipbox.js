(function (document, window, $) {
  //
  // 		jQuery is ready as $

  document.querySelectorAll(".sentec-flipbox").forEach((el) => {
    el.setAttribute("tabindex", "0");
  });

  //
  //
})(document, window, jQuery);
